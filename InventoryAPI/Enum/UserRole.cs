// File path: InventoryAPI/Enum/UserRole.cs

namespace InventoryAPI.Enum
{
    /// <summary>
    /// Defines roles for the users in the system.
    /// Can be extended in the future (e.g., Supervisor, Guest, etc.)
    /// </summary>
    public enum UserRole
    {
        Admin,
        Manager,
        Staff
    }
}
